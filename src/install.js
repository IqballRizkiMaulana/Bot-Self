const install = (prefix) => { 

	return `
	
╭──────「 *INSTALL BOT* 」
┴●pkg install bash
   ●termux-setup-storage
   ●cd /sdcard
   ●cp -r Iqbal-Rizki-main /$HOME
   ●cd
   ●cd Iqbal-Rizki-main
   ●bash install.sh
   ●npm start`
   
}
   
exports.install = install